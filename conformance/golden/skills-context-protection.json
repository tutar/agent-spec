{
  "artifact_id": "skills-context-protection.v1",
  "schema_version": "1.0",
  "scenario": "skills-context-protection",
  "constraints": [
    "repeated activation should be deduplicable",
    "compaction must preserve skill activation identity or equivalent protected state",
    "bound skill resources should remain accessible through an allowlist or equivalent mechanism"
  ],
  "object_fixtures": [
    {
      "object_name": "SkillActivationResult",
      "required_fields": ["skill_name", "wrapped", "activation_mode"]
    }
  ]
}
